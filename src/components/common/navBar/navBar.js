import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

const NavBar = () => {
  return (
    <View style={styles.navBar}>
      <Text style={styles.logoText}>HikeIt</Text>
    </View>
  );
};
const styles = StyleSheet.create({});

export default NavBar;
